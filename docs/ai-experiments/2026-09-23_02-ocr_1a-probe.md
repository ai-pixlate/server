# 2026-09-23 · ② OCR 착수 준비 실측(1a · 1b)

산출물: `runs/2026-09-23_02-ocr_1a-probe/` (git 제외 — 프로브 JSON · 오버레이 · 섹션 모음 · 원 보고서).

## 1. 질문

- OCR 고정 입력 v1이 옮긴 뒤에도 온전한가(1b).
- `requirements-ocr.txt`가 로컬 Windows CPU에서 설치·추론되는가, 고정할 버전은 무엇인가.
- 설치 버전의 출력 배열(`dt_polys` · `rec_polys` · `rec_texts` · `rec_scores`)이 같은 영역끼리 대응하는가, 인식 신뢰도 임계값이 영역을 빼는가, 빈 텍스트가 반환되는가 — `pipeline.md` 7절 영역 보존(D6) 조건.
- 영역별 점수로 무엇을 쓸 수 있는가 — `open-questions.md` #34.
- 실패 예외가 어떤 클래스로 나오는가 — #33 자료.
- v1에서 품질 표본 후보를 정하고, 4,000px 초과 섹션(#31 · #32)이 있는가.

## 2. 표본

- OCR 고정 입력 v1: ① 5차 실측 결과(34장, GS-02_014는 run3)를 실측 PC에서 `freeze-input`으로 만들어 zip으로 옮김. 섹션 112개.
- 프로브: 품질 표본 후보 **밖** 4개 섹션 — GS-02_003/01(짧은 본문) · GS-03_005/01(색 띠 제목) · GS-02_014/04(작은 글씨) · GS-02_009/06(사진 위 글자·작은 글씨).
- 품질 표본 후보 12개에는 어떤 OCR도 실행하지 않았다.

## 3. 조건

- PC: Windows 11 · Python 3.12.10 · CPU. 브랜치 `feature/ocr` @ `9722745`.
- PaddleOCR: det `PP-OCRv5_server_det` · rec `korean_PP-OCRv5_mobile_rec` · 문서 방향 분류·왜곡 보정·줄 방향 분류 끔. pipeline 코드 없이 단독 스크립트.
- 비교: 인식 신뢰도 임계값 라이브러리 기본값 vs 0 명시.
- 예외 시험: 잘못된 rec 모델명 · 없는 이미지 경로 · 0바이트 PNG. 네트워크 차단 시험은 하지 않음.

## 4. 결과

**1b — v1 수령**: zip SHA-256 실측 PC 보고와 일치 · 폴더 34 · 섹션 PNG 112 · `verify-input --relocated` 34/34 통과.

**환경**

| 항목 | 결과 |
|---|---|
| 설치 | 긴 임시 폴더 경로의 가상환경에서 실패(`OSError [Errno 2]`, 경로 길이 한도) → 짧은 경로에서 성공 |
| 버전 | paddlepaddle 3.3.1 · paddleocr 3.7.0 · paddlex 3.7.2 |
| 추론 | `predict`에서 `NotImplementedError`(oneDNN 실행기, 원인 추정) → `enable_mkldnn=False`로 동작 |
| 테스트 | 80개 중 79 통과, 실패 1 = 알려진 DejaVu 건 |
| CPU 속도 | oneDNN 끈 상태 섹션당 9~28초 |

**v1 목록**: 섹션 높이 201~3,470px(중앙 1,302), 폭 800 · 830 · 1000. **4,000px 초과 0개.**

**경계 선별**(판정 기준: 경계 위아래 행이 모두 비균일(행 표준편차 > 6.0)이면 육안 확인): 내부 경계 78개 중 2개 표시 → 육안으로 둘 다 글자 획 절단 아님(그라데이션 1 · #29 줄 간격 1). 원본 이미지 경계에서 잘린 글자 1건(GS-03_016 끝 ↔ GS-03_017 시작 "TO USE"). 전체 경계를 사람이 전수 확인한 결과가 아니다.

**프로브**(판정 기준: 배열 길이 일치 · 육안 번호 대조 불일치 0 · 두 임계값 결과 동일 여부)

| 섹션 | 영역 | 빈 텍스트 | rec_score 최소 / 하위10% / 중앙 |
|---|---|---|---|
| GS-02_003/01 | 5 | 0 | 0.963 / 0.967 / 0.999 |
| GS-03_005/01 | 22 | 0 | 0.388 / 0.965 / 1.000 |
| GS-02_014/04 | 35 | 0 | 0.628 / 0.905 / 0.959 |
| GS-02_009/06 | 13 | 0 | 0.951 / 0.962 / 0.997 |

- 4개 모두 `dt_polys` = `rec_polys` = `rec_texts` = `rec_scores` 길이, `rec_polys == dt_polys`. 기본 임계값이 0.0이라 두 실행 결과 동일.
- 육안 대조 57개 영역(GS-03_005/01 · GS-02_014/04 전수) 불일치 0.
- 출력에 `dt_scores` 없음 — 영역별 점수는 `rec_scores`뿐.
- 코드 확인(paddlex 3.7.2 `pipelines/ocr/pipeline.py` · 인식 후처리): `rec_*`는 같은 블록·같은 인덱스로 추가, 빈 인식 결과는 score 0.0 → 임계값 0에서 유지, 크기 0 잘라낸 이미지만 인식 전 제외. 빈 텍스트는 실측에서 관찰되지 않음.
- 형식 관찰: 검출 단위가 어절·줄로 섞임 · 인식 텍스트의 띄어쓰기 누락 잦음 · 비문자 검출 2건(0.388 · 0.628).

**예외**

| 시험 | 예외 | 시점 |
|---|---|---|
| 잘못된 rec 모델명 | `ValueError` ← `UnknownModelError` | 초기화 |
| 없는 이미지 | `FileNotFoundError` | predict |
| 0바이트 PNG | `cv2.error` | predict |
| oneDNN(환경) | `NotImplementedError` | predict |

네 건 모두 반복해도 같은 결과(결정적). 추론 예외 전체에 일반화하지 않는다.

## 5. 판단

- **채택**: 버전 고정(위 3종) · Windows에서만 oneDNN 끔 · 짧은 가상환경 경로 권장.
- **채택**: 영역 보존(D6) 조건 충족 — 이 버전·임계값 0 한정, 빈 텍스트는 코드 근거. 검출만 된 박스의 추가 복원은 현재 버전에서 구현하지 않음.
- **채택**: `score` = 인식 신뢰도 `rec_scores[i]`(#34). ⑥ 0.5 기준의 검증으로 확장하지 않음.
- **보류**: #33 `retryable` 세분 — 네 사례는 관찰 기록만, BE 합의 전.
- **보류**: 임시 분할(#31 · #32) — v1에 해당 섹션이 없어 실측 검증 불가. 합성 테스트와 별도 긴 이미지로 나중에 검증.
- **채택**: 품질 표본 12개 — GS-02_014/09 · GS-01_002/16 · GS-01_002/17 · GS-03_002/04 · GS-02_001/04 · GS-03_018/11 · GS-01_002/15 · GS-02_007/02 · GS-02_001/03 · GS-01_002/05 · GS-02_002/01 · GS-03_016/01.
- **채택**: 채점 명세 v2에 `source_cut` 추가(원본 경계 잘림은 입력 기인).

## 6. 반영

- `pipeline/requirements-ocr.txt` 버전 고정 · `dev.md` 6절(설치 경로 · oneDNN) — 2026-09-23 `894038c`.
- `pipeline.md` 7절(D6) · `contract.md` 2.5 · `open-questions.md` 5절 #34 · #33 관찰 · `status.md` 1·2·4절 — 2026-09-23 `d1c8bf6`.
- 채점 명세 v2 — `2026-09-23_02-ocr_scoring-spec-v2.md`.
