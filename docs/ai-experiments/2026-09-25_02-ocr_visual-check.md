# 2026-09-25 · ② OCR 육안 확인(v1 섹션 100개, GPU)

산출물: `runs/2026-09-25_02-ocr_visual-check/` (git 제외 — 섹션별 `ocr/*.json` · `run.json`, 결과 화면 `viewer/index.html`, 실행·화면 생성 스크립트).

## 1. 질문

- 구현한 ② OCR(4,000px 이하)의 결과를 사람이 눈으로 보고, 정량 기준선 없이 다음 단계(③)로 넘어가도 될 만큼 괜찮은가.
- 사용자 판단(2026-09-25): 지금은 개선보다 다음 단계 개발을 우선한다. 정답 검수 · 기준선 측정 · 개선은 나중에 한다.

## 2. 표본

- OCR 고정 입력 v1의 섹션 112개 중 **품질 표본 12개를 뺀 100개**(34장 전부에서). 품질 표본은 정답 동결 전 OCR 금지 원칙에 따라 넣지 않았다.

## 3. 조건

- 커밋 `e3a50b2`, CLI `ocr`를 섹션마다 호출(한 프로세스, 엔진 1회 로딩).
- 이 PC GPU 환경: RTX 4060 Laptop · `gpu:0` · paddle 3.3.1(CUDA 11.8) · paddleocr 3.7.0 · paddlex 3.7.2 · 인식 신뢰도 임계값 0 · oneDNN 설정은 라이브러리 기본(GPU).
- 결과 화면: 섹션 이미지 위 번호 붙은 검출 폴리곤 + 번호별 인식 텍스트·score 표. score 0.5 미만을 빨강으로 표시(보기용 구분, 품질 기준 아님).

## 4. 결과

판정 기준: 사용자가 결과 화면을 보고 OK/보류를 정한다. 정답과 비교한 점수가 아니다.

| 항목 | 값 |
|---|---|
| 섹션 | 100 — 모두 `ok`(실패 0) |
| 소요 | 37초(엔진 로딩 포함 전체) |
| 영역 | 1,815 — 섹션당 최소 1 · 중앙 13.5 · 최대 173, 영역 0개 섹션 없음 |
| score 0.5 미만 | 107 |
| 빈 텍스트 | 39 — 모두 score 0.0으로 반환·보존. 25개가 GS-02_008(논문 지면 사진 속 작은 글씨), 큰 사진 영역을 글자로 검출한 경우 포함(예: GS-01_002/11 348×344 · 789×679) |

- 빈 텍스트 39개가 score 0.0으로 남은 것은 1a에서 코드로만 확인했던 영역 보존(D6) 조건의 **실측 확인**이다(이 버전·이 설정·이 표본 범위).

## 5. 판단

- **채택(사용자 OK)**: ② 구현을 "구현 완료(4,000px 이하) · 육안 확인(섹션 100개) · 정량 기준선 보류"로 마무리하고 ③으로 넘어간다. 원래 정의한 "품질 승인"(정답 기반 기준선 이후)과는 구분한다.
- **보류**: 품질 표본 정답 검수·동결(초안 12개, 도구·채점 명세 v3 보관) → 기준선 측정 → 개선.
- **보류(알려진 한계)**: 4,000px 초과 섹션 거부(#31 · #32 미구현 — v1에는 해당 섹션이 없었을 뿐) · `retryable` 세분(#33, BE) · 워커 연결.

## 6. 반영

- `status.md` 1 · 2 · 3 · 4절(② 상태 · 다음 착수 · 세션 로그 · 인수인계), `pipeline.md` 7절(빈 텍스트 보존 실측 근거 추가) — 2026-09-25.
