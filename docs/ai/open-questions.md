# open-questions.md — 미결 · 확인 필요 · 상위 문서 수정 필요

> 근거: 개발계획 v3.6 · 스키마 계약 문서 v3.5 · PoC 검증 결과 요약 2026-09-17 · `pipeline.md` `contract.md` `db-map.md`(2026-09-18) · `dev.md`(2026-09-20) · 갱신 2026-09-22(#26 분류 · #27 · #28 · #29 · #30) · 2026-09-23(#31 · #32 · #33 · #34) · 2026-09-26(#34 재개 · #35)
> 성격: **목록**이다. 해결 방안·추천안은 적지 않는다. 닫힌 항목은 5절로 옮기고 해당 정본을 갱신한다.
> 담당: 세 정본에 적힌 것만 채웠다. 적혀 있지 않으면 `미정`.
> 상태: `미정` 결정 필요 / `BE 확인 필요` / `데이터 담당` 데이터 정의 대기. 결정 후 상위 정본에 반영해야 닫히는 항목은 상태와 별개로 3절에 등재한다.
> 12월 범위는 4절에만 둔다. MVP 항목과 섞지 않는다.

---

## 1. 차단 항목 — 번역 최종 확정과 MVP 통합 검증을 막는 것

확정 8종의 구현 착수는 막지 않는다 [PoC 13장]. 아래 항목이 풀려야 ⑧ 번역이 잠정 → 확정으로 바뀌고 통합 검증이 가능하다.

| # | 항목 | 영향 | 담당 | 상태 | 출처 |
|---|---|---|---|---|---|
| 1 | **규제 매핑 표 실제 스펙** — 요구안 5항목(제외문맥 열거 한계 · 적용 범위 · 대체표현의 직역 구별 · 대체표현 수렴 금지 · 숫자·고유명사 보존). 확정 시 ⑧ 채택 모델 재검증 | ⑧ 번역 · `contract.md` 5.1 | 데이터 담당 | 데이터 담당 | [PoC 0장 #1, 3장, 13장] [개발계획 2.1, 4.3, 6장] [계약 10.2] |

## 2. MVP 미결

### 2.1 섹션 판정·정책

| # | 항목 | 영향 | 담당 | 상태 | 출처 |
|---|---|---|---|---|---|
| 2 | 게시 대상 사이트와 사이트별 게시 정책(리뷰 · 설문 · 체험단 · 비포·애프터 등 금지 항목) | ③-1' 정책 적용 | 데이터 담당 | 데이터 담당 | [PoC 0장 #4] [개발계획 2.1] [계약 10.2] |
| 3 | 정책 데이터 구조 — 항목 on/off 방식으로 할지 | ③-1' · 캐시(정책 변경 시 재계산) | 미정 | 미정 | [PoC 0장] [`pipeline.md` 7절] |
| 4 | 판정 항목(`content_type`) 목록과 항목별 판정 기준 · 항목 경계 — 시술 비유를 효능 오인에 넣는가 · 사용법 간/자사 제품 간 비교를 타사 비교에 넣는가 · 외부 플랫폼 표시 | ③-1 · `contract.md` 4.1 | 미정 | 미정 | [PoC 0장 #5, 10-1장] [계약 4.1, 10.2] |
| 5 | ③-1 AI 섹션 판정 방식 최종 채택 — PoC 최선 후보 `sec_adj`, 맥락 함정 섹션 구성 후 재채점 | ③-1 | AI | 미정 | [PoC 13장] [개발계획 4.1, 6장] |
| 6 | 정책 적용 로직의 구현 소유자 — AI 서버 / BE | ③-1' | 미정 | 미정 | [`pipeline.md` 단계표] |
| 7 | 정책 계약의 담당 — `section_verdict` 허용값과 정책 결과의 매핑 | ③-1' · `contract.md` 4.2 | 미정 | 미정 | [계약 4.2, 10.2] [`contract.md` 10장] |
| 8 | 부적합 내용이 섹션 일부만 차지할 때 — 섹션째 제외 / 부분 가림 | ③-1' 결과를 하류가 받는 방식 | 미정 | 미정 | [PoC 0장 #6] |

### 2.2 텍스트 블록·역할

| # | 항목 | 영향 | 담당 | 상태 | 출처 |
|---|---|---|---|---|---|
| 9 | 주의문구(`caution`) 범위 — 광고 근거 각주(랭킹 기준 · 특허번호 · 효과 한정) 포함 여부. 포함 시 골든 미탐 22 → 최대 54 | ③ `role` 정의 | 미정 | 미정 | [PoC 0장 #2, 5장] [개발계획 4.1, 4.3] |
| 10 | 제품명과 효능 주장의 구분 방식 — 제품명 `role`은 추가하지 않음 | ③ · ⑧ | 미정 | 미정 | [개발계획 2.1, 4.3, 6장] [계약 2.1, 10.2] |
| 24 | ③ `source_ko` 줄 결합 방식 — 줄 사이를 공백으로 잇는지 줄바꿈을 보존하는지. ⑤ 로고 완전 일치 비교와 ⑧ 번역 입력에 영향 | ③ · ⑤ · ⑧ | 미정 | 미정 | [계약 2장, 2.3] [`dev.md` 3절] |

### 2.3 번역·인페인팅

| # | 항목 | 영향 | 담당 | 상태 | 출처 |
|---|---|---|---|---|---|
| 11 | RAG 용어집 구축 후 번역 프롬프트 기술검증 · 증분 번역 · 강제 용어 적용 검증 | ⑧ · `contract.md` 5.1 | AI | 미정 | [개발계획 4.1, 6장] [계약 5.1, 10.2] |
| 12 | ⑥에서 제외된 저신뢰·빈 텍스트 영역의 번역·렌더 처리 규칙 — 원문 보존까지만 확정 | ⑥ · ⑧ · ⑨ | 미정 | 미정 | [개발계획 6장 주석] [`pipeline.md` 7절] |

### 2.4 계약 공백 — 원본 스키마 계약에 없음

| # | 항목 | 영향 | 담당 | 상태 | 출처 |
|---|---|---|---|---|---|
| 13 | 섹션 `range` — 외부 계약값인지 `top_offset`·`height`에서 계산하는 내부값인지 | `analyze()` 출력 | 미정 | 미정 | [`contract.md` 1.1] |
| 14 | `text_block.style` JSON 키 이름 · 값 형식 · 허용값 | ⑦ · ⑨ | 미정 | 미정 | [`contract.md` 2장] |
| 15 | 출력 분할용 절단 가능 지점(섹션 경계 · 블록 여백 좌표)의 함수 · 필드 · JSON 구조 | 저장 · 출력 분할 | AI(제공)·BE(절단) | 미정 | [개발계획 3.1] [`contract.md` 10장] |
| 16 | 라벨·로고 판정 변경 시 실행 중 작업의 오래된 결과를 막는 방식 — revision 증가 · 별도 버전 · 작업 취소 | 캐시 · revision | 미정 | 미정 | [`contract.md` 9장] |
| 25 | ① 섹션 분해 VLM 호출 실패 처리 — 대체 처리(색 전환 경계만으로 진행) 허용 여부 · 오류·경고 코드 · 실패 유형별(타임아웃·일시 장애 / 인증 실패·잘못된 응답) 재시도 조건. 계약 8장은 `IMAGE_OPEN_FAILED` · `OCR_FAILED`만 정의 | `analyze()` 실패 처리 · 워커 재시도 | AI(제안)·BE(합의) | BE 확인 필요 | [계약 8장] [`contract.md` 8장] [`pipeline.md` 7절] |
| 33 | ② OCR 실패의 오류 분류와 `retryable` — 계약 8장은 "OCR 타임아웃·일시 오류 → `OCR_FAILED` 재시도 가능"만 정한다. `errors.py`의 `OCR_FAILED` 기본값은 `retryable=True`라 설정·의존성 오류처럼 반복해도 해결되지 않는 실패까지 재시도 대상이 된다. 정할 것: 섹션 이미지 열기 실패의 코드(`IMAGE_OPEN_FAILED` 여부) · 모델 초기화 실패(의존성 · 잘못된 모델명·설정 · 손상된 모델 파일)와 모델 다운로드 네트워크 오류의 구분 · 추론 예외 중 **확인된 일시 오류**와 잘못된 입력·결정적 출력 오류(출력 배열 길이 불일치 등)의 구분 · **분류되지 않은 예외**의 `retryable` · 시간 제한(현재 장치 없음, 워커 연결 시). 계약에 없는 오류 코드는 만들지 않는 방향(코드는 `OCR_FAILED`, `retryable`을 명시해 구분)이 제안돼 있다. 예외 클래스 매핑은 설치 버전 프로브(로컬 1a) 결과로 정한다. **1a 관찰(2026-09-23, paddlex 3.7.2 · Windows CPU)** — 설치 경로 오류: 긴 가상환경 경로에서 `OSError [Errno 2]`(경로 길이 한도, 설치 단계) / 환경 호환성 오류: `predict` 시 `NotImplementedError`(oneDNN 실행기, `enable_mkldnn=False`로 회피) / 이미지 오류: 없는 파일 `FileNotFoundError` · 0바이트 PNG `cv2.error`(둘 다 `predict`) / 설정 오류: 잘못된 모델명 `ValueError` ← `UnknownModelError`(초기화). 네 사례 모두 반복해도 같은 결과인 결정적 오류였으나, **이것을 추론 예외 전체를 `retryable=False`로 두는 근거로 쓰지 않는다**(네트워크·자원 오류는 시험하지 않음). **현재 구현(2026-09-23)**: 결정 전이라 `errors.ERROR_POLICY` 기본값을 쓴다 — 섹션 이미지 열기 실패 `IMAGE_OPEN_FAILED`(False), 엔진 초기화 실패 · 추론 예외 · 출력 형식 오류(반환값·배열 형식 포함) `OCR_FAILED`(True). 섹션 이미지 크기가 `Section` 메타데이터와 다르면 입력 불량으로 `IMAGE_OPEN_FAILED`(False). 즉 설정·환경 오류도 지금은 재시도 가능으로 표시된다. CLI `run.json`에는 예외 클래스를 남겨 분류 자료로 쓴다. CLI 실행 구조(섹션 오류는 계속 처리 · 공통 초기화 실패는 즉시 중단 · `run.json` 상태 집계)는 `dev.md` 4절 소관으로 별도 확정됨 | ② 실패 처리 · 워커 재시도 | AI(제안)·BE(합의) | BE 확인 필요 | [계약 8장] [`contract.md` 8장] |
| 34 | ② OCR 영역 `score`의 정의 — 계약 2.5는 영역별 `score`라는 이름만 두고 어떤 점수인지(인식 신뢰도 / 검출 신뢰도) 정의하지 않는다. 이 값은 `text_block.source_lines`로 저장되고 ⑥ 인페인팅 판정(0.5 기준)에 쓰이므로 BE·계약 소유자와 합의해야 한다. **이력**: 2026-09-23 1a 프로브(설치 버전 paddlex 3.7.2 출력에 영역별 점수는 `rec_scores`뿐, `dt_scores` 없음, 텍스트·폴리곤과 같은 인덱스) 뒤 사용자 결정으로 `score` = 인식 신뢰도(`rec_scores[i]`)로 정해 5절로 옮겼다. **2026-09-26 재개**: PR #36 검토에서 BE 인계 값의 정의를 AI 쪽 결정만으로 확정한 점이 지적됨. 사용자가 허용한 범위는 "현재 OCR 구현값(`rec_scores[i]`) 유지"이며 계약 정의로 확정된 것은 아니다(`dev.md` 3절). ⑥의 0.5 기준이 검증됐다는 뜻이 아니며 기준값도 바꾸지 않는다 | ② · ⑥ · `contract.md` 2.5 · `db-map.md` `source_lines` | AI(제안)·BE(합의) | BE 확인 필요 | [계약 2.5] [`contract.md` 2.5] [`pipeline.md` 단계표 ⑥] |
| 35 | 워커 인계 형식(`AnalyzeResult` · `analyze.json`)의 `image_path` 경로 규칙과 버전 표기 — 현재 인계 형식은 버전 `"1"`, `image_path`는 실행 시점의 경로 문자열 그대로(변환 없음)다. 개발용 `split.json`에는 2026-09-23부터 "상대 경로 = JSON 파일 폴더 기준" 규칙과 파일 버전 `"2"`(`SPLIT_SCHEMA_VERSION`)가 붙는다(`dev.md` 3절). 정할 것: (1) 워커가 받는 `image_path`가 절대 경로여야 하는지, 어떤 기준의 상대 경로를 허용하는지 (2) 인계 형식의 버전을 올릴 조건과 절차 (3) 파이프라인 파일 버전(`SCHEMA_VERSION` · `SPLIT_SCHEMA_VERSION`)과 계약의 `schema_version`(DB 저장 JSON 구조 버전, `contract.md` 4.1 · 6.1)이 이름은 같지만 다른 것임을 계약 문서에 어떻게 구분해 적을지 (4) 워커가 `analyze()` 대신 단계 함수(`section_split.run` 등)를 직접 부를 가능성 — 있다면 `SplitResult`의 파일 버전이 인계에 노출된다. 합의 전에는 인계 형식을 바꾸지 않는다(2026-09-26, PR #36 범위 축소로 인계 형식은 PR 이전 동작으로 복원) | `analyze()` 출력 · 워커 저장 · `contract.md` 1.1 · 3.1 | AI(제안)·BE(합의) | BE 확인 필요 | [계약 1.2] [`contract.md` 1.1] [`dev.md` 3절] |

### 2.5 출력 분할·렌더 연동

| # | 항목 | 영향 | 담당 | 상태 | 출처 |
|---|---|---|---|---|---|
| 17 | 높이 한도 값 — 타겟 채널 규격에서 확인 | 출력 분할 | 기획·프론트 | 미정 | [개발계획 3.1] |
| 18 | 단일 섹션이 한도를 넘는 경우의 처리 | 출력 분할 | 미정 | 미정 | [개발계획 3.1] |
| 19 | 영역 확장 시 인접 블록·섹션 재배치의 세부 규칙 | 검수 · ⑨ | FE·BE 렌더 계약 | 미정 | [개발계획 3.1] |
| 20 | 출력 장수가 입력과 달라진다는 점을 알릴 시점·방식 | 저장 | 기획·프론트 | 미정 | [개발계획 3.1] |

### 2.6 구현·레포

| # | 항목 | 영향 | 담당 | 상태 | 출처 |
|---|---|---|---|---|---|
| 26 | ① 섹션 분해 `[section]` 값의 실측 확정. 3차 실측(2026-09-22, 34장, 정답 70경계)까지의 상태 — `bg_row_ratio` · `snap_radius_px` · `color_delta`는 확정(5절). **확정 후보(단독 비교 있음)**: `blank_row_std` 6.0 — 4차 실측(34장) 8.0 단독 비교에서 색 단계 오절단 5건 추가(유형 A 3·B 2), 재생 32장 보정 차이 ±1px 6건뿐, 8.0이 이기는 경계 없음. 전체 채점 악화(69/70·10 → 66/70·20)에는 VLM 비결정성이 섞여 있어 가장 확실한 근거는 색 단계 5건. 파라미터 탐색은 "추가 조정의 이득을 확인하지 못해 종료". **기준값 유지·미검증**: `min_section_px` · `long_section_px` · `color_window_px` · `vlm_width_px` · `vlm_window_px` · `vlm_window_overlap_px`. **실험 옵션**: `vlm_seed`(-1 미사용, seed=1은 채택 근거 부족). 대안 기각은 현재값 유지 근거이지 최적값 증명이 아니다. 확정 후보는 사용자 "확정" 시 config·`pipeline.md` 3절 반영 | `pipeline.md` 3절 · `pipeline/config/default.toml` `[section]` | AI | 미정 | [`pipeline.md` 3절] [PoC 9장] |
| 27 | ① 목록 항목별 절단(FAQ·후기·Q&A, 실측 유형 B) 규칙 — 색 전환 경계 단계의 실험 가설. **구현 보류.** 4차 분석(34장) 결과: FAQ 질문 띠는 흰→노랑→회색 세 색 순환(복귀 띠 아님, 높이 123px×5). 반복 ≥3만으로는 GS-02_008 회색 상자 열(정답 3551)도 잡히고, 열 안 높이 편차 ≤15%를 더한 변형(b)이 정적으로 FAQ 2341·2741만 제거(tol 20 변형(c)도 정적 결과는 같으나 FAQ 색 차이 22.5에 2.5 여유뿐이라 (b)를 후보로 둠). 근거는 GS-03_018 한 장. 잔존 4045는 못 잡음. **채택 조건**: (1) FAQ·Q&A 표본과 같은 색·높이의 독립 주제 카드가 반복되는 정상 사례를 추가해 높이 동일성 가정 검증 (2) 2341·2741을 지워 길어지는 구간 [1647,4045](2,398px)에 VLM이 항목을 다시 제안하는지 실제 호출 확인. 정하기 전 결정할 것: 약한·강한 후보의 수치 기준, 강한 후보의 기각 사유 범위(`bg_ratio`는 사진·도형도 포함), 반복을 세는 범위(인접 열 / 이미지 전체), 강한 후보 둘을 하나의 띠로 짝짓는 방법과 실제 띠 높이 분포(GS-03_018 질문 띠 123px), 같은 색 쌍 유사성 기준, 독립 주제가 같은 패턴을 반복할 때의 보호 조건. 채택은 실제 VLM 포함 34장 전체를 정답 목록으로 채점해 판단 | `pipeline/stages/section_split.py` · `pipeline.md` 3절 | AI | 미정 | [`pipeline.md` 3절] |
| 28 | ① VLM 경계 선택 프롬프트 개정 — 4차 기준 실행에서 VLM 몫의 오류는 오절단 GS-03_018 7493 · 누락 GS-03_002 4601, 그리고 실행마다 오가는 것들(GS-01_002 꼬리 3건 · GS-02_008 1840 · GS-02_014 2891/15185 · GS-03_002 7573/10229). 제외 예시 추가와 함께 포함 기준(고유 소제목이 있는 독립 주제)을 보강. **실험 설계**: 현행 프롬프트(비교군) · 제외 예시 추가 · 제외+포함 보강의 3조건을 VLM 호출 이미지(4차 기준 20장)에 각 3회. 좌표 일치보다 정답 경계 검출 빈도·오절단 빈도·실행별 편차를 비교(3회 반복은 흔들림 관찰의 최소치이지 비결정성 제거가 아님). "N회 호출 다수결"은 비용 N배의 설계 변경이라 별도 실험. 원인에 창 절단·입력 해상도·모델 한계가 섞여 있어 프롬프트만으로 해결된다고 가정하지 않음 | `pipeline/prompts/section_boundary.md` | AI | 미정 | [`pipeline.md` 3절] |
| 29 | ① 그라데이션 띠 안의 색 후보와 줄 간격 보정에 따른 **제목 분리** — 5차 실측(2026-09-22, 34장) GS-02_014 1건. 노란 띠(8834~, 정답 경계)는 500px에 걸친 세로 그라데이션(B 채널 86→160)이라 뚜렷한 배경 전환이 없는데, 제목 두 줄 위에서 후보 9234(narrow 15.6 · wide 43.8)가 확정된다. 글자 위 경계 보정은 여백 높이를 보지 않아 20px 줄 간격 (9180, 9200)을 목적지로 택해 제목 1행은 위, 2행은 아래 섹션이 된다(획 절단은 없음). 정답 기준(제목은 아래 본문과 같은 섹션)으로 올바른 결과는 후보 기각이고, 절단을 남긴다면 제목 위 여백 끝 9116(반경 40 밖, 118px)이며 그래도 한계 절단이다. **구현 보류**: 표본 1건이라 임계값을 정할 자료가 없다. 5차에서 제안된 규칙 후보(미채택): (1) 보정 목적지 여백의 최소 높이(이 지점 줄 간격 20 · 제목 위 여백 54 · 아래 37 — 임계 40이면 기각, 30이면 제목이 위 섹션으로) (2) 여백 간격이 인접 글자 띠 높이의 절반 미만이면 한 글자 블록으로 보고 블록 위 끝으로 이동 (3) narrow/wide 비와 균일 행 중앙값 기울기로 그라데이션 후보 자체를 기각. 필요 자료: 34장에서 보정 목적지가 된 여백 구간의 높이 분포, 그라데이션 띠 안에 큰 글자가 있는 다른 사례 | `pipeline/stages/section_split.py` · `pipeline.md` 3절 | AI | 미정 | [`pipeline.md` 3절] |
| 30 | ① 확정 창의 이웃 절단이 1px 이동에 뒤집힘 — `_neighbor_bounds`가 `min_gap`(= `color_window_px`×4 = 32px)보다 가까운 이웃 후보를 건너뛰는데, 후보가 보정으로 1px 움직이면 "31px(건너뜀)"이 "32px(포함)"으로 바뀌어 확정 창이 달라진다. 5차 실측 GS-02_014 8626→8625에서 뒤 창이 [y, 8670)→[y, 8657)로 줄어 wide 12.1→1.7, `median_delta` 기각(기록된 사유는 색 차이지만 실제 원인은 창 변화). 이 표본에서는 오절단 제거로 이로웠고 정상 경계 손실은 확인되지 않았다. 4차 5절 "창 절단 우연"과 같은 계열이며 #29(제목 분리)와는 다른 문제. **보류 이유**: 현재 표본에서 피해가 확인되지 않았고, 고칠 경우 수정 효과와 34장 영향 범위를 따로 검증해야 한다. 재현: `split --source pipeline/samples/local/GS-02/GS-02_014.jpg --set section.long_section_px=999999`, `split_debug.json` `color_candidates` `y_raw=8626` | `pipeline/stages/section_split.py` | AI | 미정 | [`pipeline.md` 3절] |
| 31 | ② OCR 임시 분할(4,000px 초과 섹션)의 타일 구간 규칙 — 정본은 "여백 우선 · 없으면 2,000px 띠 300px 겹침"만 정한다. 제안 규칙(미확정): (1) `H ≤ ocr.split_threshold_px`면 분할 없음(4000 분할 없음, 4001 분할) (2) 커서 s에서 명목 끝 e = s + `ocr.tile_px`, e ≥ H면 마지막 타일 [s, H) (3) 탐색 창 [e − `ocr.tile_overlap_px`, e)에서 e에 가장 가까운 균일 행 구간을 고르고(창 경계에 걸치면 창 안쪽 부분만), 구간 [r0, r1]의 c = r0 + (r1 − r0) // 2를 절단선으로, 없으면 c = e (4) 현재 타일 [s, c), 다음 타일 s' = c − `tile_overlap_px` — **여백 분할에도 겹침을 둔다.** 여백에 놓이는 것은 현재 타일의 끝뿐이고 다음 타일 시작은 글자 위일 수 있다 (5) H − c < `tile_overlap_px`면 현재 타일을 [s, H)로 연장(타일 최대 높이 = `tile_px + tile_overlap_px`). override 안전 조건: `tile_px > 2 × tile_overlap_px` · `tile_overlap_px ≥ 0` · `tile_px ≤ split_threshold_px` — 어기면 커서가 정체·후퇴할 수 있으므로 config 로드 시 거부. 균일 행 기준: ① `section.blank_row_std` 재사용(①을 조정하면 ② 타일링도 바뀌는 결합) 대신 **OCR 전용 키 `ocr.blank_row_std`** 신설 제안(초기값은 현재 ①과 같은 값, 잠정). 키 추가는 별도 승인 | `pipeline/stages/ocr.py` · `pipeline.md` 3절 · `pipeline/config/default.toml` `[ocr]` | AI | 미정 | [계약 3.2] [`pipeline.md` 단계표 ②] |
| 32 | ② OCR 타일 겹침 중복 정리 절차와 기준값 — 정본은 "중첩 영역의 중복 검출을 정리"만 정한다. 제안 절차(미확정): 겹침 띠에 걸친 영역만 대상 (1) **서로 다른 타일 사이에만** 대응 간선: 세로 겹침 비율 v = 교집합 높이 / min(높이) ≥ Tv 이고 가로 교집합 > 0. 텍스트는 대응에 쓰지 않는다(다른 줄의 같은 문구가 합쳐지지 않게) (2) 성분 유효성: 각 타일 쪽에서 쌍마다 v ≥ Tv · 같은 쪽 가로 겹침 비율(교집합 폭 / min(폭)) ≤ Th · 행 높이 비(그쪽 성분 높이 / 그쪽 최대 영역 높이) ≤ Rh — 어기면 불확실 (3) 대표는 영역 하나가 아니라 **한쪽 타일의 검출 묶음 전체**(양쪽을 섞지 않음). 자동 선택은 가로 덮음(그쪽 x 합집합 / 성분 전체 x 합집합) 차이 ≤ Δc일 때만: 내부 절단선 Ec px 안 접촉이 없는 쪽 → 성분 중심의 겹침 띠 중앙선 소유 (4) 덮음 차이 > Δc면 불확실 (5) 한쪽 타일에만 있는 영역은 잘렸더라도 보존 (6) 불확실 성분은 양쪽 모두 보존하고 진단(`ocr_debug.json`: 타일 구간 · 성분 구성 · 분류 · 선택 규칙 · 버린 영역 · 접촉 표시)에 기록. **한계**: 가장자리 접촉은 잘림의 증거가 아니라 의심 신호다. 덮음 동등성은 선택 휴리스틱이며 글자 보존을 보장하지 않는다 — 가로 합집합 길이가 같아도 덮는 위치가 다를 수 있고, 같은 박스에서도 인식 텍스트가 다를 수 있으므로 실측으로 검증한다. 기준값 Tv · Th · Rh · Δc · Ec는 이름만 있고 값 없음. 순서: 절차와 키 이름·잠정 후보값 승인 → 합성 사례 + 4,000px 초과 섹션 전체 이음매로 override 비교 → 사용자 "확정" 후 기본값 반영. 등록은 구현 승인이 아니다 | `pipeline/stages/ocr.py` · `pipeline.md` 3절 · `pipeline/config/default.toml` `[ocr]` | AI | 미정 | [계약 3.2] [`pipeline.md` 단계표 ②] |
| 23 | BE 정본 ERD의 경로·버전, `db-map.md` 참조 색인의 테이블·컬럼명 일치 여부 | `db-map.md` 전체 | BE | BE 확인 필요 | [`db-map.md` 3절] |

## 3. 상위 문서 수정 필요

| # | 문서 | 수정 내용 | 출처 |
|---|---|---|---|
| A | 스키마 계약 문서 v3.5 | 근거 표기를 "개발계획 v3.6"으로 | [계약 머리] |
| B | 스키마 계약 문서 v3.5 | 계약 공백 #13 · #14 · #15 · #16 · #25 · #33 · #34 · #35가 **결정된 뒤** 반영, #7 정책 계약 담당 명시 | [`contract.md` 10장] |

## 4. 12월 범위 — MVP 밖

MVP 완성 후 다룬다. 상세는 여기 두지 않는다.

- 규격 변경 경로 전체 — 후속 번역 파생(`trans_2`) · 템플릿 할당·매핑 · 템플릿 렌더링 · 규격 자동 검증 · 아마존 A+ 산출 [개발계획 2.3, 4.1] [계약 10.1]
- 레이어 분해 · 정밀 누끼 — `birefnet-general` + `vlm_pick` 잠정, 그래픽 잔존·대상 누락·VLM 번호 착오 재검증 · 제품컷 누끼를 기능에 남길지 [PoC 0장 #3, 4장, 13장] [개발계획 4.1]
- 썸네일 입력 · 입력 유형 축(상세페이지/썸네일 선택) [개발계획 2.1, 3.1]
- 로고 인식 고도화 — 등록 로고 파일 · 이미지 매칭 · 심볼형 로고 · 매칭 실패 신호 [개발계획 2.1, 4.1] [계약 2.3, 10.1]
- 인페인팅 품질 고도화 — 잔존율 측정 · 재시도 기준 [개발계획 4.1] [계약 5.2, 10.1]
- 품질 신호 집계·분석 — 검수 수정 전후 기록 집계 [개발계획 4.1]
- 출력 분할 용량 한도 — 규격 검증에 용량 항목 도입 시 [개발계획 3.1]
- 미국 외 국가·언어 [개발계획 4.3]

## 5. 해결 이력

닫힌 항목은 번호 · 결정 · 반영한 정본 · 날짜를 적고 위 표에서 지운다.

| # | 결정 | 반영 | 날짜 |
|---|---|---|---|
| 3절 C | 조항 번호 안정성 규칙은 개발계획이 아니라 문서 운영 규칙으로 둠 | `README.md` 4.3 | 2026-09-19 |
| 26(일부) | ① `bg_row_ratio` 0.25 · `snap_radius_px` 40 · `color_delta` 12를 **현재 MVP 기본값**으로 확정. 근거는 3차 실측(34장, 정답 70경계)의 대안 비교(0.35 · 80 · 20/24 기각). 변경 금지가 아니며 새 표본에서 문제가 확인되면 비교 실험을 거쳐 바꾼다. 검증 범위는 34장 | `pipeline/config/default.toml` · `pipeline.md` 3절 | 2026-09-22 |
| 21 | ① 섹션 분해 VLM 모델명 `gemini-3.8-flash`(사용자 확정) | `pipeline.md` 3절 · `pipeline/config/default.toml` `section.vlm_model` | 2026-09-21 |
| 22 | AI 코드 `pipeline/`, config `pipeline/config/default.toml`(키 = `pipeline.md` 3절 키), 프롬프트 `pipeline/prompts/` | `dev.md` 1·2절 · `pipeline.md` 헤더·3절·⑧행·9절 · `README.md` 3·4·6절 | 2026-09-20 |
