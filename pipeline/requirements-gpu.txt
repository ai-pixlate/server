# Stage 6 inpainting - LaMa (iopaint). GPU server (gpu/) only. torch/torchvision come from the gpu/Dockerfile image.
# Not yet install-verified. Pin the versions that work on the server and mirror them in gpu/Dockerfile.
-r requirements-ocr.txt
iopaint
