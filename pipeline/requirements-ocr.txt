# Stage 2 text extraction - PaddleOCR PP-OCRv5. Runs on CPU locally.
# Pinned to the versions verified on 2026-09-23 (Windows 11, Python 3.12.10, CPU). See docs/ai/dev.md section 6.
# Windows: install into a short venv path (Long Path limit); CPU inference needs enable_mkldnn=False (dev.md 6).
-r requirements.txt
numpy
paddlepaddle==3.3.1
paddleocr==3.7.0
paddlex==3.7.2
