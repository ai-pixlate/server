# Stage 2 text extraction - PaddleOCR PP-OCRv5. Runs on CPU locally.
# Not yet install-verified. Pin the versions that work on first install (see docs/ai/status.md).
-r requirements.txt
numpy
paddlepaddle
paddleocr
