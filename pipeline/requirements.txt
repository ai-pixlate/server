# AI pipeline - local base (types, CLI, overlay, tests, stage 1 section split). No local models.
# BE stack (FastAPI, DB, Celery) lives in the root requirements.txt; workers install both.
# Comments are ASCII on purpose: some pip versions decode this file with the locale encoding (cp949 on Korean Windows).
pydantic>=2,<3
pillow>=10
numpy>=1.26          # stage 1 row profile
google-genai>=1      # stage 1 VLM boundary picking (API key: GEMINI_API_KEY env var). Imported lazily at call time.
pytest
